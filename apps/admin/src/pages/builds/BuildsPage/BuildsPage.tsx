import { PagePlaceholder } from "../../../shared/ui/PageState/index.js";

export function BuildsPage() {
  return (
    <PagePlaceholder
      description="Build status will be connected to remote state in a later session."
      title="Builds"
    />
  );
}
