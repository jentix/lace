---
import type { BlockContext, PublicBlock } from "../lib/rendering.js";
import { readDocument } from "../lib/rendering.js";
import RichText from "./RichText.astro";

interface Props {
  readonly block: PublicBlock;
  readonly context: BlockContext;
}

const { block, context } = Astro.props;
const document = readDocument(block, context, "content");
---

<section class="rich-text"><RichText document={document} /></section>
